-- =========================================
-- Create Department table
-- =========================================

CREATE TABLE Department (
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(50),
    location VARCHAR(50)
);



-- Insert data into Department

INSERT INTO Department VALUES
(1, 'IT', 'Mumbai'),
(2, 'HR', 'Pune'),
(3, 'Finance', 'Delhi'),
(4, 'Marketing', 'Mumbai');



-- =========================================
-- Create Employee table
-- =========================================

CREATE TABLE Employee (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    salary INT,
    dept_id INT
);



-- Insert data into Employee

INSERT INTO Employee VALUES
(101, 'John', 50000, 1),
(102, 'Amit', 60000, 1),
(103, 'Neha', 45000, 2),
(104, 'Rahul', 70000, 3),
(105, 'Sneha', 55000, 4),
(106, 'Kiran', 40000, 2);



-- =========================================
-- a) Employees who work in IT department
-- =========================================

SELECT e.emp_id,
       e.emp_name,
       e.salary
FROM Employee e
JOIN Department d
ON e.dept_id = d.dept_id
WHERE d.dept_name = 'IT';



-- =========================================
-- b) Employees whose salary is greater than
-- average salary of all employees
-- =========================================

SELECT *
FROM Employee
WHERE salary >
(
    SELECT AVG(salary)
    FROM Employee
);



-- =========================================
-- c) Employees who work in same department
-- as 'John'
-- =========================================

SELECT *
FROM Employee
WHERE dept_id =
(
    SELECT dept_id
    FROM Employee
    WHERE emp_name = 'John'
);



-- =========================================
-- d) Department name of employee having
-- highest salary
-- =========================================

SELECT d.dept_name
FROM Employee e
JOIN Department d
ON e.dept_id = d.dept_id
WHERE e.salary =
(
    SELECT MAX(salary)
    FROM Employee
);



-- =========================================
-- e) Employees working in departments
-- located in Mumbai using IN
-- =========================================

SELECT *
FROM Employee
WHERE dept_id IN
(
    SELECT dept_id
    FROM Department
    WHERE location = 'Mumbai'
);



-- =========================================
-- f) Employees who earn more than ANY
-- employee in department 2
-- =========================================

SELECT *
FROM Employee
WHERE salary > ANY
(
    SELECT salary
    FROM Employee
    WHERE dept_id = 2
);